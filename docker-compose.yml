services:
  app:
    build: .
    volumes:
      - .:/app             
    ports:
      - "8501:8501"        # streamlit
    environment:
      - STREAMLIT_SERVER_RUN_ON_SAVE=true
    command: ["python", "-m", "streamlit", "run", "view/app.py", "--server.port=8501", "--server.address=0.0.0.0"]